# .github/workflows/test-checkout.yml
name: Test Checkout
on: [push]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "✅ Checkout worked successfully!"
